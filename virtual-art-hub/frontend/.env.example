# 本地开发：默认 http://127.0.0.1:5002，可用 REACT_APP_PROXY_TARGET 指向后端。
# REACT_APP_PROXY_TARGET=http://127.0.0.1:5002

# 仅当前端与 API 不同源时设置（默认用浏览器当前访问的 origin + /api，适合 HTTP 同源反代）。
# REACT_APP_API_BASE_URL=http://your-api-host:5002/api
